# Example env vars for Resume Tailor Framework
OPENAI_API_KEY=sk-REPLACE_ME
OPENAI_MODEL=gpt-4o-mini

# Optional: only if you use Azure, OpenRouter, or local LLM with OpenAI-compatible API
# OPENAI_BASE_URL=https://your-endpoint/v1

# Demo mode returns mock bullets (no LLM calls)
DEMO_MODE=0
